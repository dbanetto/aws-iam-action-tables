# IAM action tables scrapper

Cause sometimes there are stuff inside the IAM actions table that is just no
where else (like the access type... or condition keys... or resources... or
dependent actions).
